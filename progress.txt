## Codebase Patterns
- Rust toolchain 1.90.0 with edition 2024; cargo is at `~/.cargo/bin/cargo.exe` and must be added to PATH in bash
- Root crate is `memvid-core` (package name); workspace members listed in root Cargo.toml `[workspace]` section
- NAPI-RS v2 used for Node.js bindings (napi = "2" with features ["napi9", "tokio_rt"], napi-derive = "2", napi-build = "2")
- Async NAPI methods use `tokio::task::spawn_blocking` for blocking Rust calls; requires `tokio = { version = "1", features = ["rt"] }` in Cargo.toml
- `JsMemvid` wraps `Arc<Mutex<Option<Memvid>>>` — `guard_memvid!` macro checks for None (CLOSED) and poisoned mutex (INTERNAL)
- `Memvid` is `Send` — safe to move across threads via `spawn_blocking`
- `pub(crate) use guard_memvid;` re-exports the macro for use in sibling modules
- `lock_inner()` function in `memvid.rs` provides non-macro guard for closures where `return Err` doesn't work
- The `MEMVID_CORE_VERSION` constant in `src/lib.rs` provides the version string
- Default features for memvid-core: `lex`, `pdf_extract`, `simd`
- All 335 lib tests pass with `cargo test -p memvid-core --lib`
- `MemvidError` (in `src/error.rs`) has 40+ variants; each maps to a string code in `bindings/node/src/error.rs`
- Feature-gated variants (e.g., `temporal_track`) require a passthrough feature in `bindings/node/Cargo.toml`
- NAPI test runner outputs "Load Node-API ... failed" warnings — these are harmless when running `cargo test` outside Node.js
- Each binding module gets its own file (e.g., `write.rs`) with `#[napi] impl JsMemvid` block; register via `pub mod write;` in `lib.rs`
- NAPI `Buffer` auto-accepts JS `Buffer` and `Uint8Array`; `i64` maps to JS `bigint` for frame IDs
- NAPI `Float32Array` from `napi::bindgen_prelude` maps to JS `Float32Array`; use `.to_vec()` to get `Vec<f32>`
- `FrameRole` has 3 variants: `Document`, `DocumentChunk`, `ExtractedImage` — re-exported from `memvid_core::FrameRole`
- For Rust structs with many fields, use `Option<T>` in JS `#[napi(object)]` structs so omitted fields get Rust defaults
- `#[napi(object)]` structs don't derive `Debug` — use `match` pattern (not `.unwrap_err()`) for `Result<JsStruct, _>` in tests
- Rust `u128` (e.g., `elapsed_ms`) maps to `f64` in JS since NAPI doesn't support u128
- `BlobReader` implements `Read + Seek`; constructors are private — can only be created via `Memvid::blob_reader()` / `blob_reader_by_uri()`
- NAPI-RS cannot easily create Node.js Readable streams from Rust (no `require()` access in native addon context); return a pull-based reader class instead and document Readable wrapping in JS
- `MemoryCardBuilder` requires `source_frame_id` and `engine` even for JS API — use placeholder values (`source(0, None)`, `engine("node-api", "1.0.0")`) for user-facing card insertion
- `LogicMeshStats` is exported from `types::mod.rs` but NOT re-exported from `lib.rs` — access via method return type or `memvid_core::types::LogicMeshStats`
- `AskContextFragment` and `AskContextFragmentKind` are NOT re-exported from `memvid_core` root — use `memvid_core::types::ask::` path
- For generic `Option<&E>` embedder params, create a `NoEmbedder` dummy impl and pass `None::<&NoEmbedder>`
- `EntityKind::from_label("product")` maps to `Project` (not `Product`) — "product" is an alias for Project in the Rust crate
- `start_enrichment_worker` takes `Arc<Mutex<Memvid>>` (not `Option<Memvid>`) — for JsMemvid, use `run_worker_loop` directly with closures that handle `Option::None`
- `EnrichmentWorkerHandle`, `run_worker_loop`, `TaskResult` are in `pub mod enrichment_worker` — accessible externally even if not re-exported from `lib.rs`
- `tracing` is not a dep of `memvid-node` — use `let _ = expr;` instead of `tracing::warn!`
- `serde_json = "1"` is a direct dep of `memvid-node` for converting `serde_json::Value` metadata to JS-compatible types
- `ReaderRegistry` has no top-level `extract` — use `find_reader(&hint)` then `reader.extract(bytes, &hint)`
- Enum↔string conversion for `#[serde(rename_all = "snake_case")]` enums: use `serde_json::to_value` / `serde_json::from_value(Value::String(...))` — requires `serde = "1"` as a direct dep for trait bounds

---

## 2026-02-25 - US-001
- Scaffolded NAPI-RS project under `bindings/node/`
- Files created:
  - `bindings/node/Cargo.toml` — crate `memvid-node`, cdylib, depends on memvid-core + napi v2
  - `bindings/node/build.rs` — napi_build::setup()
  - `bindings/node/src/lib.rs` — minimal `#[napi] pub fn version() -> String`
  - `bindings/node/package.json` — `@memvid/node`, napi targets for linux-x64-gnu, darwin-x64, darwin-arm64, win32-x64-msvc
  - `bindings/node/.gitignore` — excludes *.node, target/, node_modules/, generated index.js/index.d.ts
- Root `Cargo.toml` updated: added `[workspace] members = ["bindings/node"]`
- **Learnings for future iterations:**
  - Workspace setup: just add `[workspace]` with `members` above the existing `[package]` — the root crate is implicitly a member
  - napi-build v2 requires a `build.rs` with `napi_build::setup()`
  - `cargo check -p memvid-node` and `cargo check -p memvid-core` both pass — workspace doesn't break existing crate
  - All 335 existing tests still pass after workspace conversion
---

## 2026-02-25 - US-002
- Implemented `NapiMemvidError` struct and error code mapping in `bindings/node/src/error.rs`
- Files created/changed:
  - `bindings/node/src/error.rs` — `NapiMemvidError` struct, `error_code()` mapping all 40+ `MemvidError` variants to string codes, `error_path()` extractor, `from_memvid_error()` conversion, `catch_unwind_napi()` panic catcher, 10 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod error;`
  - `bindings/node/Cargo.toml` — added `[features] temporal_track = ["memvid-core/temporal_track"]` passthrough
- **Learnings for future iterations:**
  - `MemvidError` has ~40 variants in `src/error.rs`; string codes like `IO`, `LOCKED`, `CHECKSUM_MISMATCH`, etc.
  - Feature-gated enum variants (e.g., `#[cfg(feature = "temporal_track")]`) need matching passthrough features in bindings crate Cargo.toml
  - `napi::Error::new(Status::GenericFailure, msg)` is the standard way to throw JS errors from Rust
  - `catch_unwind` requires the closure to be `UnwindSafe` — `std::panic::catch_unwind` works for pure Rust code; for closures capturing references use `AssertUnwindSafe`
  - NAPI test runner produces "Load Node-API" warnings outside Node.js but tests run fine
---

## 2026-02-25 - US-003
- Implemented `JsMemvid` lifecycle wrapper in `bindings/node/src/memvid.rs`
- Files created/changed:
  - `bindings/node/src/memvid.rs` — `JsMemvid` struct wrapping `Arc<Mutex<Option<Memvid>>>`, sync and async factory methods (`createSync`/`create`, `openSync`/`open`, `openReadOnlySync`/`openReadOnly`), `close()`, `isReadOnly`/`path` getters, `guard_memvid!` macro, `lock_inner()` helper, 3 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod memvid;`
  - `bindings/node/Cargo.toml` — added `tokio_rt` feature to napi, added `tokio` dependency with `rt` feature
- **Learnings for future iterations:**
  - `Memvid` is `Send` — all its fields (File, PathBuf, FileLock, Arc<RwLock<...>>, etc.) are Send-safe
  - NAPI-RS async factory methods: use `#[napi(factory, js_name = "create")]` + `async fn` — napi-rs handles Promise creation automatically
  - No explicit `Memvid::close()` in Rust — uses `Drop` which auto-commits if dirty; Node binding wraps in `Option` and calls `.take()` for explicit close
  - `MutexGuard<Option<Memvid>>` doesn't impl `Debug` (because `Memvid` doesn't) — use `match` instead of `.unwrap_err()` in tests
  - The `guard_memvid!` macro uses early `return Err(...)` so it only works in functions returning `napi::Result<T>`, not in closures — use `lock_inner()` for closures
---

## 2026-02-25 - US-004
- Wrapped basic write operations: `putBytes`, `commit`, `commitSkipIndexes`, `finalizeIndexes`, `beginBatch`, `endBatch`
- Files created/changed:
  - `bindings/node/src/write.rs` — sync and async variants of `putBytes`, `commit`, `commitSkipIndexes`, `finalizeIndexes`; sync `beginBatchSync`/`endBatchSync`; `JsBatchOptions` object; 4 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod write;`
- **Learnings for future iterations:**
  - NAPI `Buffer` type auto-accepts both `Buffer` and `Uint8Array` from JavaScript — no special handling needed
  - Frame IDs are `u64` in Rust but NAPI-RS `#[napi]` methods use `i64` for bigint interop (napi-rs maps `i64` to JS `bigint`)
  - `Memvid::put_bytes(&mut self, &[u8]) -> Result<u64>` is the main write API; `Buffer` derefs to `&[u8]` via `AsRef`
  - `Memvid::begin_batch(PutManyOpts)` / `end_batch()` for batch mode; `PutManyOpts` has fields: `compression_level`, `disable_auto_checkpoint`, `skip_sync`, `wal_pre_size_bytes`, etc.
  - Pattern: each new binding module goes in its own file (e.g., `write.rs`), adds `#[napi] impl JsMemvid` block, and is registered via `pub mod write;` in `lib.rs`
  - The `memvid-core --lib` tests have pre-existing failures on Windows due to Tantivy temp dir access contention that poisons a serial test mutex — unrelated to node bindings
---

## 2026-02-25 - US-005
- Wrapped PutOptions, putBytesWithOptions, putWithEmbedding, and deleteFrame operations
- Files changed:
  - `bindings/node/src/write.rs` — added `JsPutOptions` struct with `#[napi(object)]`, `to_put_options()` conversion, `parse_frame_role()` helper, sync/async variants of `putBytesWithOptions`, `putWithEmbedding`, `deleteFrame`; 6 new unit tests (total 23 passing)
  - `prd.json` — marked US-005 as `passes: true`
- **Learnings for future iterations:**
  - `PutOptions` has many fields; expose them as `Option<T>` in the JS struct so omitted fields get Rust defaults
  - `FrameRole` is an enum with 3 variants (`Document`, `DocumentChunk`, `ExtractedImage`) — accept as string and parse
  - `Float32Array` from `napi::bindgen_prelude` auto-maps to JS `Float32Array`; use `.to_vec()` to get `Vec<f32>`
  - `Memvid::delete_frame(frame_id) -> Result<u64>` returns the tombstone frame's sequence ID
  - `beginBatchSync` / `endBatchSync` were already implemented in US-004 — the US-005 acceptance criteria mentioning them was already satisfied
---

## 2026-02-25 - US-006
- Wrapped search with SearchRequest and SearchResponse types
- Files created/changed:
  - `bindings/node/src/search.rs` — `JsSearchRequest`, `JsSearchHit`, `JsSearchResponse` structs with `#[napi(object)]`; `to_search_request()` and `from_search_hit()`/`from_search_response()` converters; `searchSync`/`search` methods on `JsMemvid`; 6 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod search;`
  - `prd.json` — marked US-006 as `passes: true`
- **Learnings for future iterations:**
  - `SearchRequest` has no `Default` impl — must construct all fields manually; use sensible defaults (top_k=10, snippet_chars=200)
  - `SearchEngineKind` enum maps to strings: `tantivy`, `lex_fallback`, `hybrid`
  - `elapsed_ms` is `u128` in Rust — use `f64` for JS since NAPI doesn't support u128 natively
  - `SearchHit.matches` (usize) maps to `matchCount` (u32) in JS for clarity
  - `SearchHit.score` is `Option<f32>` in Rust, widened to `Option<f64>` for JS
  - `SearchRequest` requires `#[cfg(feature = "lex")]` — enabled by default via memvid-core default features
  - `#[napi(object)]` structs don't auto-derive `Debug` — use `match` pattern in tests instead of `.unwrap_err()` for Result types containing them
  - ACL fields (`acl_context`, `acl_enforcement_mode`) and temporal fields are not exposed in JS — use Rust defaults
---

## 2026-02-25 - US-007
- Wrapped timeline queries: `JsTimelineQuery`, `JsTimelineEntry`, `timelineSync`, `timeline`
- Files created/changed:
  - `bindings/node/src/timeline.rs` — `JsTimelineQuery` and `JsTimelineEntry` structs with `#[napi(object)]`, `to_timeline_query()` and `from_timeline_entry()` converters, `timelineSync`/`timeline` methods on `JsMemvid`, 6 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod timeline;`
  - `prd.json` — marked US-007 as `passes: true`
- **Learnings for future iterations:**
  - `TimelineQuery.limit` is `Option<NonZeroU64>` in Rust — accept `Option<i64>` from JS and convert via `u64::try_from` + `NonZeroU64::new` (0 or negative → None)
  - `TimelineEntry.child_frames` is `Vec<FrameId>` (u64) — map each to `i64` for JS bigint interop, same as frame_id
  - `Memvid::timeline(&mut self, query: TimelineQuery) -> Result<Vec<TimelineEntry>>` is the core API
  - `TimelineEntry` has a `#[cfg(feature = "temporal_track")] temporal` field — set to `None` in the converter for now, not exposed to JS
  - Pattern continues: each binding module in its own file with `#[napi] impl JsMemvid` block, registered via `pub mod` in `lib.rs`
---

## 2026-02-25 - US-008
- Wrapped frame metadata access: `JsFrame`, `frameByIdSync`/`frameById`, `frameByUriSync`/`frameByUri`, `frameCount` getter, `nextFrameId` getter
- Files created/changed:
  - `bindings/node/src/frame.rs` — `JsFrame` struct with `#[napi(object)]`, `from_frame()` converter, `role_to_string()`/`encoding_to_string()` helpers, sync/async variants of `frameById`/`frameByUri`, `frameCount`/`nextFrameId` getters, 8 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod frame;`
  - `prd.json` — marked US-008 as `passes: true`
- **Learnings for future iterations:**
  - `Memvid::frame_by_id(&self, FrameId)` and `frame_by_uri(&self, &str)` take `&self` (not `&mut self`) — use `guard.as_ref().unwrap()` for sync, and `guard.as_ref().unwrap()` in `lock_inner` closures for async
  - `Frame` struct has 29 fields but the JS `JsFrame` only exposes the most useful subset: id, timestamp, kind, track, uri, title, tags, labels, extra_metadata, role, canonical_encoding, search_text
  - `BTreeMap<String, String>` (extra_metadata) converts to `HashMap<String, String>` for NAPI which maps to `Record<string, string>` in TypeScript
  - `FrameRole`, `CanonicalEncoding` enums converted to lowercase strings matching serde `rename_all = "snake_case"` convention
  - `frame_count()` returns `usize` — cast to `u32` for JS `number`; `next_frame_id()` returns `u64` — cast to `i64` for JS `bigint`
  - Don't import types only used in tests into the main module — use `use memvid_core::FrameStatus;` inside `mod tests` block instead
---

## 2026-02-25 - US-009
- Wrapped frame payload and text retrieval: `frameCanonicalPayload`, `frameTextById`, `framePreviewById`, `frameEmbedding`, `frameContext`
- Files created/changed:
  - `bindings/node/src/payload.rs` — `JsFrameContext` struct with `#[napi(object)]`, sync and async variants of all 5 methods, 6 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod payload;`
  - `prd.json` — marked US-009 as `passes: true`
- **Learnings for future iterations:**
  - `frame_canonical_payload(&mut self, FrameId) -> Result<Vec<u8>>` — returns raw bytes, convert to `Buffer` via `.into()`
  - `frame_text_by_id` returns full text (no truncation), `frame_preview_by_id` returns truncated preview — different use cases
  - `frame_embedding(&mut self, FrameId) -> Result<Option<Vec<f32>>>` — returns None if vec index not enabled; map `Vec<f32>` to `Float32Array::new(vec)`
  - `frame_context(&mut self, FrameId, &str) -> Result<(String, usize)>` — returns tuple of (text, match_count); wrap in `JsFrameContext` object for JS
  - All frame payload methods take `&mut self` (unlike `frame_by_id` which takes `&self`) — use `guard.as_mut().unwrap()` for sync, `guard.as_mut().unwrap()` in `lock_inner` closures for async
  - Pattern: result objects like `JsFrameContext` that are simple enough don't need separate converter functions — construct inline
---

## 2026-02-25 - US-010
- Wrapped blobReader as streaming reader: `JsBlobReader` class with `readChunk`, `length`, `close`; `blobReader`/`blobReaderByUri` methods on `JsMemvid`
- Files created/changed:
  - `bindings/node/src/stream.rs` — `JsBlobReader` struct wrapping `Mutex<Option<BlobReader>>`, `readChunk(size)` returning `Buffer | null`, `length` getter, `close()` method, `blobReader(frameId)`/`blobReaderByUri(uri)` sync methods on `JsMemvid`, 6 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod stream;`
  - `prd.json` — marked US-010 as `passes: true`
- **Learnings for future iterations:**
  - `BlobReader` wraps either a cloned `File` handle (for `Plain` encoding) or an in-memory `Cursor<Vec<u8>>` (for `Zstd`); both are `Send`
  - `BlobReader::from_file` / `from_memory` constructors are `pub(crate)` — cannot create BlobReader directly in unit tests; test via closed-instance patterns
  - NAPI-RS has no built-in Node.js Readable stream support; `require('stream')` is inaccessible from native addon context (module-scope variable, not on globalThis)
  - Pull-based `readChunk(size)` pattern works well: returns `Buffer` for data, `null` for EOF, throws on error — maps directly to Readable's `_read` protocol
  - `BlobReader::len()` returns `u64` — cast to `i64` for JS bigint-safe range; exposed as `length` getter
  - `std::io::Read::read()` returns `Ok(0)` at EOF — maps to returning `None` (null in JS) to signal stream end
---

## 2026-02-25 - US-011
- Wrapped memory card CRUD operations: `JsMemoryCard`, `putMemoryCardSync`/`putMemoryCard`, `putMemoryCardsSync`/`putMemoryCards`, `memoryCardCount` getter, `clearMemoriesSync`/`clearMemories`
- Files created/changed:
  - `bindings/node/src/memory.rs` — `JsMemoryCard` struct with `#[napi(object)]`, `to_memory_card()` and `from_memory_card()` converters, sync and async variants of `putMemoryCard`, `putMemoryCards`, `clearMemories`, `memoryCardCount` getter, 6 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod memory;`
  - `prd.json` — marked US-011 as `passes: true`
- **Learnings for future iterations:**
  - `MemoryCard` has many fields but the JS API only exposes the core fields (kind, entity, slot, value, polarity, event_date) — internal fields like source_frame_id, engine, confidence are set to defaults
  - `MemoryCardBuilder` requires `source_frame_id` and `engine` — use placeholder values (`source(0, None)` and `engine("node-api", "1.0.0")`) since these are internal tracking fields
  - `MemoryKind::from_str()` falls back to `Other` for unknown strings — no need for validation error handling
  - `Polarity::from_str()` returns `Option<Polarity>` — `None` for unknown strings, only set polarity if valid
  - `put_memory_card(&mut self, card)` and `put_memory_cards(&mut self, cards)` return `Result<MemoryCardId>` / `Result<Vec<MemoryCardId>>` — card IDs are `u64`, map to `i64` for JS bigint
  - `clear_memories(&mut self)` has no return value — just returns `()`, wrap in `Ok(())` for napi
  - `memory_card_count(&self)` takes `&self` not `&mut self` — use `guard.as_ref().unwrap()` for sync
  - `from_memory_card()` helper marked `pub(crate)` with `#[allow(dead_code)]` — will be used by US-012 memory query module
---

## 2026-02-25 - US-012
- Wrapped memory card query methods: `getCurrentMemorySync`/`getCurrentMemory`, `getEntityMemoriesSync`/`getEntityMemories`, `getMemoryTimelineSync`/`getMemoryTimeline`, `getPreferencesSync`/`getPreferences`, `aggregateMemorySlotSync`/`aggregateMemorySlot`, `memoriesStats`
- Files changed:
  - `bindings/node/src/memory.rs` — added `JsMemoriesStats` struct with `#[napi(object)]`, sync and async variants of all 5 query methods plus `memoriesStats` getter, removed `#[allow(dead_code)]` from `from_memory_card()`, 6 new unit tests (total 67 passing)
  - `prd.json` — marked US-012 as `passes: true`
- **Learnings for future iterations:**
  - All memory query methods on `Memvid` take `&self` (not `&mut self`) — use `guard.as_ref().unwrap()` for sync, same in `lock_inner` closures for async
  - `MemoriesStats` fields: `card_count`, `entity_count`, `slot_count` (all `usize`), `cards_by_kind` (`HashMap<String, usize>`), `enriched_frames` (`usize`), `last_enrichment` (`Option<i64>`) — cast `usize` to `u32` for JS
  - `HashMap<String, u32>` in `#[napi(object)]` maps to `Record<string, number>` in TypeScript — works out of the box
  - `get_current_memory` returns `Option<&MemoryCard>` — maps to `JsMemoryCard | null` in JS via `Option<JsMemoryCard>`
  - `from_memory_card()` was already prepared in US-011 as `pub(crate)` — removed `#[allow(dead_code)]` now that it's actively used
  - Pattern confirmed: use `match` instead of `.unwrap_err()` in tests for `Result<T, _>` where `T` contains `#[napi(object)]` structs (no `Debug` impl)
---

## 2026-02-25 - US-013
- Wrapped memory card schema operations: `JsPredicateSchema`, `JsValidationResult`, `registerSchemaSync`/`registerSchema`, `validateCardSync`/`validateCard`, `setSchemaStrictSync`, `inferSchemasSync`/`inferSchemas`
- Files created/changed:
  - `bindings/node/src/schema.rs` — `JsPredicateSchema` and `JsValidationResult` structs with `#[napi(object)]`, `from_predicate_schema()`/`to_predicate_schema()` converters, sync and async variants of all methods, 10 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod schema;`
  - `bindings/node/src/memory.rs` — changed `to_memory_card()` visibility from private to `pub(crate)` for reuse in schema module
  - `prd.json` — marked US-013 as `passes: true`
- **Learnings for future iterations:**
  - `PredicateSchema` has 8 fields: id, name, description, domain, range, cardinality, inverse, builtin
  - `ValueType` is a tagged enum with 7 variants — for JS, flatten into `range` (string tag) + `range_entity_kind` + `range_enum_values` optional fields
  - `EntityKind` has 11 variants (Person, Organization, Project, Email, Date, Location, Product, Event, Money, Url, Other) — use `EntityKind::from_label()` to parse from strings
  - `validate_card(&self, &MemoryCard) -> Result<(), SchemaError>` returns `SchemaError` (not `MemvidError`) — map to `{ valid: false, error: string }` instead of throwing
  - `set_schema_strict(&mut self, bool)` takes `&mut self`; `infer_schemas(&self)` takes `&self`
  - `to_memory_card()` in memory.rs was made `pub(crate)` for cross-module reuse — pattern for sharing converters between binding modules
  - All 76 binding tests pass (10 new for schema module)
---

## 2026-02-25 - US-014
- Wrapped logic mesh entity graph operations: `JsMeshNode`, `JsMeshEdge`, `JsFollowResult`, `JsLogicMeshStats` structs with `#[napi(object)]`; sync and async variants of `addMeshNode`/`addMeshNodes`, `addMeshEdge`/`addMeshEdges`, `follow`, `findEntity`, `frameEntities`, `entitiesByKind`, `logicMeshStats`
- Files created/changed:
  - `bindings/node/src/mesh.rs` — all mesh JS types, converters (`to_mesh_node`, `from_mesh_node`, `to_mesh_edge`, `from_mesh_edge`, `from_follow_result`), sync/async methods on `JsMemvid`, 12 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod mesh;`
  - `prd.json` — marked US-014 as `passes: true`
- **Learnings for future iterations:**
  - `MeshNode`, `MeshEdge`, `FollowResult` are in `memvid_core` (re-exported from `types::logic_mesh`); `LogicMeshStats` is in `types` module but NOT re-exported from `lib.rs` — access via `logic_mesh_stats()` return type
  - `EntityKind::from_label("product")` maps to `Project` (not `Product`) — "product" is treated as an alias for `Project` in the Rust crate
  - `MeshNode::new()` takes `(canonical_name, display_name, kind, confidence_f32, frame_id, byte_start, byte_len)` — byte spans default to (0, 0) for API-injected nodes
  - All mesh query methods on `Memvid` take `&self` (not `&mut self`) — use `guard.as_ref().unwrap()` for read-only access
  - Mesh mutation methods (`add_mesh_node`, `add_mesh_nodes`, `add_mesh_edge`, `add_mesh_edges`) take `&mut self` and don't return `Result` — they set `dirty = true` internally
  - `LinkType::from_str()` supports many aliases ("manages" → Manager, "works_at" → Employer, etc.) and falls back to `Custom(String)` for unknown types
  - `from_memvid_error` import not needed for mesh module since mutation methods return `()` not `Result`
  - All 88 binding tests pass (12 new for mesh module)
---

## 2026-02-25 - US-015
- Wrapped ask/RAG operations: `JsAskRequest`, `JsAskResponse`, `JsAskCitation`, `JsAskContextFragment`, `JsAskStats` structs with `#[napi(object)]`; `askSync`/`ask` methods on `JsMemvid`; `NoEmbedder` placeholder for the generic embedder parameter
- Files created/changed:
  - `bindings/node/src/ask.rs` — all ask JS types, converters (`to_ask_request`, `from_ask_citation`, `from_ask_context_fragment`, `from_ask_response`, `search_response_to_js`), mode/retriever string helpers, `NoEmbedder` impl, sync/async `ask` methods, 10 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod ask;`
  - `prd.json` — marked US-015 as `passes: true`
- **Learnings for future iterations:**
  - `AskContextFragment` and `AskContextFragmentKind` are NOT re-exported from `memvid_core` root — access via `memvid_core::types::ask::AskContextFragment` / `AskContextFragmentKind`
  - `Memvid::ask()` takes generic `Option<&E>` where `E: VecEmbedder` — use `None::<&NoEmbedder>` with a dummy impl to satisfy the type parameter for lex-only mode
  - `AskMode` default is `Hybrid`; accepted strings: "lex", "sem"/"semantic", "hybrid"
  - `AskRetriever` is an output-only enum (not set by user) — represents which retriever was actually used at runtime
  - `AskStats` uses `u128` for millisecond fields — cast to `f64` for JS (same pattern as SearchResponse.elapsed_ms)
  - `AskCitation.chunk_range` is `Option<(usize, usize)>` — flatten to separate `chunk_range_start`/`chunk_range_end` fields for JS (NAPI can't represent tuple types)
  - `SearchResponse` conversion is duplicated from `search.rs` to avoid making `from_search_response` pub — could be refactored to `pub(crate)` in future
  - All 98 binding tests pass (10 new for ask module)
---

## 2026-02-25 - US-016
- Wrapped sketch-based fast search: `buildAllSketchesSync`/`buildAllSketches`, `findSketchCandidatesSync`/`findSketchCandidates`, `hasSketches`, `sketchStats`
- Files created/changed:
  - `bindings/node/src/sketch.rs` — `JsSketchTrackStats` struct with `#[napi(object)]`, `variant_to_string()`/`parse_variant()` helpers, `from_sketch_track_stats()` converter, sync and async variants of `buildAllSketches`/`findSketchCandidates`, `hasSketches`/`sketchStats` methods, 8 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod sketch;`
  - `prd.json` — marked US-016 as `passes: true`
- **Learnings for future iterations:**
  - `build_all_sketches(&mut self, SketchVariant) -> usize` takes `&mut self` — uses `guard.as_mut().unwrap()` for sync
  - `find_sketch_candidates(&self, &str, Option<SketchSearchOptions>) -> Vec<SketchCandidate>` takes `&self` — uses `guard.as_ref().unwrap()` for read-only access
  - `SketchVariant` has 3 variants (Small, Medium, Large) — accept as string and parse, default to Small for unknown strings
  - `SketchTrackStats` fields (`entry_count`, `size_bytes`, `short_text_count`) are `u64` — use `f64` for JS since these can be large numbers but don't need bigint precision
  - `SketchSearchOptions` has `Default` impl — use `..Default::default()` to fill in hamming_threshold and min_score when only overriding max_candidates
  - `SketchCandidate.frame_id` is `FrameId` (u64) — map to `i64` for JS bigint interop, same pattern as other frame IDs
  - The acceptance criteria asks for `findSketchCandidates` returning `bigint[]` (frame IDs) — simplified from full candidate objects since the sketch is a pre-filter before BM25/vector reranking
  - `from_memvid_error` not needed for this module since sketch methods don't return `Result` (except through guard_memvid macro)
  - All 106 binding tests pass (8 new for sketch module)
---

## 2026-02-25 - US-017
- Wrapped enrichment worker lifecycle: `JsEnrichmentHandle` class with `stats()`, `stop()`, `isRunning` getter; `startEnrichmentWorker(config)` on `JsMemvid`; engine-based enrichment tracking methods `getUnenrichedFramesSync`/`getUnenrichedFrames`, `recordEnrichmentSync`/`recordEnrichment`, `isFrameEnrichedSync`/`isFrameEnriched`
- Files created/changed:
  - `bindings/node/src/enrichment.rs` — `JsEnrichmentWorkerConfig`, `JsEnrichmentWorkerStats` structs with `#[napi(object)]`, `JsEnrichmentHandle` class with `stats()`, `stop()`, `isRunning`; custom `start_worker_for_js()` that adapts `run_worker_loop` for `Arc<Mutex<Option<Memvid>>>`; sync and async variants of enrichment tracking methods; 10 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod enrichment;`
  - `prd.json` — marked US-017 as `passes: true`
- **Learnings for future iterations:**
  - `start_enrichment_worker()` takes `Arc<Mutex<Memvid>>` but `JsMemvid.inner` is `Arc<Mutex<Option<Memvid>>>` — cannot use it directly; wrote custom `start_worker_for_js()` using `run_worker_loop` from `memvid_core::enrichment_worker` module (which is `pub`)
  - `EnrichmentWorkerHandle`, `run_worker_loop`, `TaskResult` are in `pub mod enrichment_worker` so accessible from bindings even though not re-exported from `lib.rs`
  - Worker closures handle `Option::None` gracefully — if instance is closed during worker operation, the get_next_task closure returns `None` which stops the worker loop
  - `EnrichmentHandle` has a private `thread` field so cannot be constructed externally — `JsEnrichmentHandle` stores `EnrichmentWorkerHandle` + `JoinHandle` separately
  - `record_enrichment()` requires `card_ids: Vec<MemoryCardId>` — pass empty `vec![]` for the node API since card IDs are an internal tracking detail
  - `EnrichmentWorkerStats` fields are `u64`/`usize` — map to `f64` for JS (same pattern as sketch stats)
  - `tracing` crate is not a dependency of `memvid-node` — use `let _ = expr;` to silently discard errors instead of `tracing::warn!`
  - All 115 binding tests pass (10 new for enrichment module)
---

## 2026-02-25 - US-018
- Wrapped document reader registry and built-in readers: `JsReaderRegistry` class with `newDefault()` factory, `extractSync`/`extract` methods; `JsReaderOutput`, `JsReaderHint`, `JsReaderDiagnostics` structs; `previewChunksSync`/`previewChunks` methods on `JsMemvid`
- Files created/changed:
  - `bindings/node/src/reader.rs` — `JsReaderRegistry` class wrapping `Arc<Mutex<ReaderRegistry>>`, `JsReaderOutput`/`JsReaderHint`/`JsReaderDiagnostics` structs with `#[napi(object)]`, `format_from_filename()` helper, `build_hint()` to construct `ReaderHint` from JS hint + magic bytes, `metadata_to_map()` for serde_json::Value to HashMap conversion, `from_reader_output()` converter, sync/async `extract` methods on registry, sync/async `previewChunks` on JsMemvid, 12 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod reader;`
  - `bindings/node/Cargo.toml` — added `serde_json = "1"` dependency for metadata conversion
  - `prd.json` — marked US-018 as `passes: true`
- **Learnings for future iterations:**
  - `ReaderRegistry` is `Send + Sync` (all `DocumentReader` impls are `Send + Sync`) — safe to wrap in `Arc<Mutex<>>` for async
  - `ReaderRegistry::default()` registers 6 built-in readers: PdfReader, DocxReader, XlsxReader, XlsReader, PptxReader, PassthroughReader
  - `ReaderRegistry` has no top-level `extract` method — use `find_reader(&hint)` to get a reader, then call `reader.extract(bytes, &hint)`
  - `ReaderHint` uses lifetimes (`<'a>`) for zero-copy slices — build it locally from owned JS strings using borrows
  - `ReaderOutput.document.metadata` is `serde_json::Value` — flatten to `HashMap<String, String>` for JS by stringifying non-string values
  - `ExtractedDocument.text` is `Option<String>` — use `.unwrap_or_default()` for JS since null text is rare and empty string is more ergonomic
  - `ReaderOutput.document.mime_type` is the detected format — use as the `format` field in JS output
  - `preview_chunks(&self, &[u8]) -> Option<Vec<String>>` takes `&self` not `&mut self` — use `guard.as_ref().unwrap()`
  - Added `serde_json = "1"` as direct dependency for `metadata_to_map()` — memvid-core re-exports `serde_json::Value` in its types but the crate itself needs the import
  - `PassthroughReader` (registered last) acts as fallback for text/markdown/html/unknown formats — extract with no format hint will use it
  - All 127 binding tests pass (12 new for reader module)
---

## 2026-02-25 - US-019
- Wrapped verification, doctor, vacuum, and stats: `JsVerificationReport`, `JsVerificationCheck`, `JsDoctorOptions`, `JsDoctorFinding`, `JsDoctorActionPlan`, `JsDoctorPhasePlan`, `JsDoctorPlan`, `JsDoctorActionReport`, `JsDoctorPhaseReport`, `JsDoctorPhaseDuration`, `JsDoctorMetrics`, `JsDoctorReport`, `JsStats` structs with `#[napi(object)]`; static sync/async `verify`/`doctor`/`doctorPlan`/`doctorApply` methods; instance `vacuumSync`/`vacuum`, `statsSync`/`stats` methods
- Files created/changed:
  - `bindings/node/src/maintenance.rs` — all verification/doctor/stats JS types, enum↔string converters via serde, `from_*` and `to_*` bidirectional converters for DoctorPlan roundtripping, `tier_to_string` helper, static and instance methods on `JsMemvid`, 14 unit tests
  - `bindings/node/src/lib.rs` — added `pub mod maintenance;`
  - `bindings/node/Cargo.toml` — added `serde = "1"` dependency for enum trait bounds in generic helpers
  - `prd.json` — marked US-019 as `passes: true`
- **Learnings for future iterations:**
  - `Memvid::verify()`, `doctor()`, `doctor_plan()`, `doctor_apply()` are all static methods (no `&self`) — use plain `#[napi(js_name = "...")]` without `factory` attribute
  - All doctor/verification enums use `#[serde(rename_all = "snake_case")]` — leverage serde serialization for enum↔string conversion: `serde_json::to_value(&enum)` → `Value::String`, `serde_json::from_value(Value::String(s))` → enum
  - `DoctorActionDetail` is a tagged enum (`#[serde(tag = "type")]`) — serialize to JSON string for the JS API, deserialize back for `doctor_apply` roundtrip
  - `DoctorPlan` must be roundtrippable (JS → Rust) for `doctor_apply` — implement full bidirectional converters with `to_doctor_plan()` / `from_doctor_plan()`
  - `Stats` has 25 fields; `u64` fields use `f64` for JS (same pattern as other modules), `Tier` enum manually mapped to strings
  - `serde = "1"` added as direct dependency for the `Serialize`/`DeserializeOwned` trait bounds in generic enum helper functions
  - `#[napi(object)]` structs still don't derive `Debug` — use `match` pattern in tests for `Result<T, _>` where `T` is a JS struct
  - All 141 binding tests pass (14 new for maintenance module)
---
