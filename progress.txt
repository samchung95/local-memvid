## Codebase Patterns
- Rust toolchain 1.90.0 with edition 2024; cargo is at `~/.cargo/bin/cargo.exe` and must be added to PATH in bash
- Root crate is `memvid-core` (package name); workspace members listed in root Cargo.toml `[workspace]` section
- Default features for memvid-core: `lex`, `pdf_extract`, `simd`
- All 335 lib tests pass with `cargo test -p memvid-core --lib`
- `MemvidError` (in `src/error.rs`) has 40+ variants; each maps to a string code in `bindings/node/src/error.rs` — reuse the same mapping for Python
- `Memvid` is `Send` — safe to move across threads
- Feature-gated variants (e.g., `temporal_track`) require a passthrough feature in bindings Cargo.toml
- Root `.gitignore` has `*.json` — any new JSON files need explicit `!path/to/file.json` negation patterns
- Node bindings at `bindings/node/` serve as the reference implementation — Python bindings should mirror the same API surface
- Each binding module gets its own file (e.g., `write.rs`, `search.rs`) with impl block; register via `pub mod write;` in `lib.rs`
- `MemoryCardBuilder` requires `source_frame_id` and `engine` — use placeholder values for user-facing card insertion
- `LogicMeshStats` is NOT re-exported from `lib.rs` — access via method return type
- `AskContextFragment` and `AskContextFragmentKind` are NOT re-exported from `memvid_core` root — use `memvid_core::types::ask::` path
- For generic `Option<&E>` embedder params, create a `NoEmbedder` dummy impl and pass `None::<&NoEmbedder>`
- `ReaderRegistry` has no top-level `extract` — use `find_reader(&hint)` then `reader.extract(bytes, &hint)`
- Enum↔string conversion: use `serde_json::to_value` / `serde_json::from_value(Value::String(...))` for `#[serde(rename_all = "snake_case")]` enums
- `start_enrichment_worker` takes `Arc<Mutex<Memvid>>` (not `Option<Memvid>`) — use `run_worker_loop` directly with closures
- PyO3 0.23 `PyErr::new` requires owned values in the args tuple (not references); use `(message.clone(),)` not `(&message,)`
- Custom exception attributes: use `err.value(py).setattr("name", value)` after `PyErr::new`
- `guard_memvid!` macro: acquires Mutex lock and checks `Option` is `Some`; export with `pub(crate) use guard_memvid;` for use in other modules
- PyO3 `__exit__` should return `PyResult<bool>` (false = don't suppress exceptions), not `Py<PyNone>`
- Avoid naming a Rust module file `memvid.rs` — it conflicts with `#[pymodule] fn memvid`; use `lifecycle.rs` instead
- PyO3 `multiple-pymethods` feature (+ `inventory` crate) is required to have multiple `#[pymethods]` blocks for the same `#[pyclass]` across files
- Write operations need mutable guard: `let mut lock = guard_memvid!(mut self, py); lock.as_mut().unwrap()`
- `#[pyo3(get)]` on `Vec<pyclass>` in frozen classes fails — use `#[getter]` returning `Vec<PyClass>` with raw Rust data stored internally
- `PutOptions::builder()` + `.build()` for put options; `PutManyOpts { ..Default::default() }` for batch options
- `cargo test -p memvid-python` fails with DLL_NOT_FOUND (cdylib needs Python runtime); use `cargo check`/`cargo build` for validation
- `BlobReader` from memvid-core uses `std::io::Read`/`std::io::Seek` traits — its `read()` returns `io::Result`, not `memvid_core::Result`; use `from_io_error` helper

---

## 2026-02-28 - US-001
- Scaffolded PyO3 + maturin project under bindings/python/
- Files changed:
  - Created bindings/python/pyproject.toml (maturin build config, project name=memvid, python>=3.9)
  - Created bindings/python/Cargo.toml (package=memvid-python, lib name=memvid, cdylib, pyo3 0.23)
  - Created bindings/python/src/lib.rs (minimal #[pymodule] with version() function)
  - Created bindings/python/.gitignore (target/, *.so, *.pyd, *.dll, __pycache__/, *.egg-info/)
  - Modified root Cargo.toml (added [workspace] members = ["bindings/python"])
  - Modified .gitignore (added !prd.json and !progress.txt negation patterns)
- **Learnings for future iterations:**
  - PyO3 0.23 uses `&Bound<'_, PyModule>` for pymodule function signatures (not `_py: Python<'_>, m: &PyModule`)
  - Main branch does not have [workspace] section — must add it when creating first binding on a fresh branch
  - The `*.txt` and `*.json` root gitignore patterns require negation for prd.json and progress.txt
  - `cargo build -p memvid-python` and `cargo check -p memvid-python` both pass clean
---

## 2026-02-28 - US-002
- Defined MemvidError Python exception and error code mapping
- Files changed:
  - Created bindings/python/src/error.rs (MemvidError exception, error_code/error_path mapping, from_memvid_error, catch_panic, register, 8 unit tests)
  - Modified bindings/python/src/lib.rs (added `pub mod error;` and `error::register(m)?;`)
- **Learnings for future iterations:**
  - `PyErr::new::<T, _>((message.clone(),))` — must pass owned values in the args tuple, not references (PyO3 0.23 requires `'static` for the args)
  - `create_exception!(memvid, MemvidError, PyException)` creates the exception type; register on module with `m.add("MemvidError", m.py().get_type::<MemvidError>())?`
  - Custom attributes (.message, .code, .path) are set via `err.value(py).setattr(...)` after creating the PyErr
  - `catch_unwind(AssertUnwindSafe(f))` catches Rust panics at the PyO3 boundary; downcast to `&str` or `String` for the message
  - No node bindings exist yet — the PRD references `bindings/node/src/error.rs` but the dir is absent; error codes were mapped directly from `memvid_core::MemvidError` variants
  - Feature-gated match arms (e.g., `#[cfg(feature = "temporal_track")] InvalidTemporalTrack`) work correctly with exhaustive matching
---

## 2026-02-28 - US-003
- Wrapped Memvid lifecycle: create, open, open_read_only, close with context manager support
- Files changed:
  - Created bindings/python/src/lifecycle.rs (PyMemvid pyclass, guard_memvid! macro, create/open/open_read_only static methods, close, is_read_only/path properties, __enter__/__exit__)
  - Modified bindings/python/src/error.rs (added closed_error helper, rustfmt reformatted)
  - Modified bindings/python/src/lib.rs (added `pub mod lifecycle;` and `lifecycle::register(m)?;`)
- **Learnings for future iterations:**
  - `guard_memvid!` macro acquires `Mutex<Option<Memvid>>` lock and checks `is_none()` — returns `MutexGuard` that callers use via `.as_ref().unwrap()` or `.as_mut().unwrap()`
  - PyO3 `__exit__` should return `PyResult<bool>` (false = don't suppress), not `Py<PyNone>` — `py.None()` returns `Py<PyAny>` which doesn't coerce
  - Don't name a Rust module file `memvid.rs` when the `#[pymodule]` function is also `fn memvid` — causes naming conflict; use `lifecycle.rs` instead
  - `Memvid` has no explicit `close()` in Rust — RAII/Drop handles cleanup; Python wrapper sets `Option` to `None` to signal closed
  - `Memvid::create`, `::open`, `::open_read_only` all take `impl AsRef<Path>` so `&str` works directly
  - `error::closed_error(py, detail)` creates a `MemvidError` with code `CLOSED` — added as a reusable helper
---

## 2026-02-28 - US-004
- Wrapped basic write operations: put_bytes, commit, commit_skip_indexes, finalize_indexes
- Files changed:
  - Created bindings/python/src/write.rs (put_bytes, commit, commit_skip_indexes, finalize_indexes methods on PyMemvid)
  - Modified bindings/python/src/lib.rs (added `pub mod write;`)
  - Modified bindings/python/Cargo.toml (added `multiple-pymethods` feature to pyo3)
- **Learnings for future iterations:**
  - PyO3 requires the `multiple-pymethods` feature to split `#[pymethods]` across multiple files for the same `#[pyclass]` — this pulls in the `inventory` crate
  - PyO3 `&[u8]` parameter automatically accepts both Python `bytes` and `bytearray`
  - Write methods (`put_bytes`, `commit`, etc.) need `let mut lock = guard_memvid!(mut self, py)` to get mutable access through the MutexGuard
  - `Memvid::put_bytes` returns `Result<u64>` (frame ID); maps directly to Python `int`
---

## 2026-02-28 - US-005
- Wrapped PutOptions, put_bytes_with_options, put_with_embedding, delete_frame, begin_batch, end_batch
- Files changed:
  - Modified bindings/python/src/write.rs (added put_bytes_with_options, put_with_embedding, delete_frame, begin_batch, end_batch)
- **Learnings for future iterations:**
  - `PutOptions::builder()` provides a fluent builder; set scalar fields via builder, then mutate `tags`/`labels` directly on the built struct
  - `PutManyOpts` has no builder — construct with struct literal + `..Default::default()`
  - `FrameRole` uses `#[serde(rename_all = "snake_case")]` — parse from Python string via `serde_json::from_value(Value::String(...))`
  - `#[pyo3(signature = (data, *, ...))]` makes all args after `*` keyword-only in Python
  - `begin_batch` takes `PutManyOpts`, `end_batch` takes no args — PRD kwargs map to PutManyOpts fields
  - `cargo test -p memvid-python` fails with DLL_NOT_FOUND on cdylib — this is expected; use `cargo check` and `cargo build` for validation
---

## 2026-02-28 - US-006
- Wrapped search with SearchRequest and SearchResponse types
- Files changed:
  - Created bindings/python/src/search.rs (PySearchHit, PySearchResponse pyclasses, search() method on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod search;` and `search::register(m)?;`)
- **Learnings for future iterations:**
  - `#[pyo3(get)]` on `Vec<pyclass>` does NOT work in frozen classes — PyO3 can't create Python references from Rust-heap pyclasses; store raw Rust data and use `#[getter]` method returning `Vec<PyClass>` instead
  - `SearchResponse.elapsed_ms` is `u128` in Rust — cast to `u64` for Python since PyO3 doesn't support u128
  - `SearchHit.matches` (Rust) maps to `match_count` (Python PRD field name) — rename in the conversion
  - `SearchEngineKind` is an enum with `Tantivy`, `LexFallback`, `Hybrid` — convert to snake_case strings for Python
  - `SearchRequest` has feature-gated `temporal` field (`#[cfg(feature = "temporal_track")]`) — must conditionally include it in struct literal
  - `SearchRequest` also has `as_of_frame`, `as_of_ts`, `acl_context`, `acl_enforcement_mode` — set to None/default for basic search wrapper
---

## 2026-02-28 - US-007
- Wrapped timeline queries with TimelineEntry pyclass and timeline() method on PyMemvid
- Files changed:
  - Created bindings/python/src/timeline.rs (PyTimelineEntry pyclass, timeline() method on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod timeline;` and `timeline::register(m)?;`)
- **Learnings for future iterations:**
  - `TimelineEntry` fields map directly to simple Python types — `Vec<u64>` for `child_frames` works with `#[pyo3(get)]` since it's not a Vec of pyclasses
  - `TimelineQuery` has a `limit: Option<NonZeroU64>` — accept `Option<u64>` from Python and convert via `NonZeroU64::new`
  - `TimelineQuery` has feature-gated `temporal` field like `SearchRequest` — use `#[cfg(feature = "temporal_track")]` in struct literal
  - Simple stories with no complex type conversions can reuse the `From` trait pattern cleanly
---

## 2026-02-28 - US-008
- Wrapped frame metadata access: PyFrame pyclass, frame_by_id, frame_by_uri, frame_count, next_frame_id
- Files changed:
  - Created bindings/python/src/frame.rs (PyFrame pyclass with all PRD fields, frame_by_id/frame_by_uri methods, frame_count/next_frame_id properties, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod frame;` and `frame::register(m)?;`)
- **Learnings for future iterations:**
  - `Frame.extra_metadata` is `BTreeMap<String, String>` in Rust — convert to `HashMap<String, String>` via `.into_iter().collect()` for the Python wrapper
  - `CanonicalEncoding` and `FrameRole` enums need manual match arms for string conversion (not serde-based since they're not `rename_all` compatible for this use)
  - `frame_by_id` and `frame_by_uri` only need immutable guard (no `mut`) since they're read-only operations
  - `frame_count` returns `usize`, `next_frame_id` returns `u64` — both map directly to Python `int`
  - `extra_metadata` uses `#[getter]` method (not `#[pyo3(get)]`) to return a cloned HashMap on each access — avoids lifetime issues with frozen pyclass
---

## 2026-02-28 - US-009
- Wrapped frame payload and text retrieval: frame_canonical_payload, frame_text_by_id, frame_preview_by_id, frame_embedding, frame_context
- Files changed:
  - Created bindings/python/src/payload.rs (PyFrameContext pyclass, 5 methods on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod payload;` and `payload::register(m)?;`)
- **Learnings for future iterations:**
  - All frame payload/text methods on `Memvid` take `&mut self` — need mutable guard (`guard_memvid!(mut self, py)` + `lock.as_mut().unwrap()`)
  - `frame_context` returns `(String, usize)` tuple — wrap in a `PyFrameContext` pyclass with `text` and `match_count` fields
  - `frame_canonical_payload` returns `Vec<u8>` — return as `PyBytes` using `PyBytes::new(py, &bytes)` with lifetime-bound return type `Bound<'py, PyBytes>`
  - `frame_embedding` returns `Option<Vec<f32>>` — maps directly to Python `list[float] | None`
  - Simple story: no complex type conversions, just direct wrapping of Rust methods
---

## 2026-02-28 - US-010
- Wrapped blob_reader as iterable/file-like object with BlobReader pyclass
- Files changed:
  - Created bindings/python/src/blob.rs (PyBlobReader pyclass with read/close/context manager/iteration, blob_reader/blob_reader_by_uri methods on PyMemvid, register function)
  - Modified bindings/python/src/error.rs (added from_io_error helper for std::io::Error conversion)
  - Modified bindings/python/src/lib.rs (added `pub mod blob;` and `blob::register(m)?;`)
- **Learnings for future iterations:**
  - `BlobReader` implements `std::io::Read` and `std::io::Seek` — `read()` returns `io::Result<usize>`, not `memvid_core::Result`; need a separate `from_io_error` helper
  - Wrap `BlobReader` in `Mutex<Option<BlobReader>>` (same pattern as PyMemvid) to support `close()` setting it to `None`
  - `BlobReader::len()` is available before any reads — capture it at construction time as `total_len` for the `length` property
  - `__next__` returning `Option<T>` maps to Python's `StopIteration` — return `Ok(None)` when `read()` returns 0 bytes
  - `blob_reader` and `blob_reader_by_uri` on `Memvid` take `&mut self` — need mutable guard
  - 64 KiB chunk size is a reasonable default for iteration
---

## 2026-02-28 - US-011
- Wrapped memory card CRUD operations: put_memory_card, put_memory_cards, memory_card_count property, clear_memories
- Files changed:
  - Created bindings/python/src/memory.rs (put_memory_card, put_memory_cards, memory_card_count, clear_memories methods on PyMemvid)
  - Modified bindings/python/src/lib.rs (added `pub mod memory;`)
- **Learnings for future iterations:**
  - `MemoryCardBuilder` requires `source_frame_id` and `engine`/`engine_version` — for user-facing Python API, use placeholders: `source(0, None)` and `engine("python-api", "1.0.0")`
  - `MemoryKind::from_str` is available on the core type but returns `Other` for invalid strings — for Python API, validate explicitly and raise `ValueError` for unknown kinds
  - `Polarity::from_str` returns `Option<Polarity>` — `None` means invalid, map to `ValueError`
  - `put_memory_cards` takes `Vec<MemoryCard>` — accept `list[dict]` from Python and parse each dict with helper functions (`dict_get_str`, `dict_get_opt_str`, `dict_get_opt_i64`)
  - `memory_card_count` is a read-only property (`#[getter]`) — uses immutable guard, returns `usize`
  - `clear_memories` takes `&mut self` in Rust — needs mutable guard in Python wrapper
  - No new pyclass types needed for US-011 — only the write/count/clear operations; PyMemoryCard will be needed for US-012 (query return values)
---

## 2026-02-28 - US-012
- Wrapped memory card query methods: get_current_memory, get_entity_memories, get_memory_timeline, get_preferences, aggregate_memory_slot, memories_stats
- Files changed:
  - Created bindings/python/src/memory_query.rs (PyMemoryCard, PyMemoriesStats pyclasses, 6 query methods on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod memory_query;` and `memory_query::register(m)?;`)
- **Learnings for future iterations:**
  - Memory query methods (`get_current_memory`, `get_entity_memories`, etc.) all return references (`&MemoryCard` / `Vec<&MemoryCard>`) — implement `From<&MemoryCard>` (not owned) for the PyMemoryCard conversion
  - `MemoriesStats.cards_by_kind` is `HashMap<String, usize>` — use the same `raw_*` + `#[getter]` pattern as PyFrame's `extra_metadata` for HashMap fields in frozen pyclasses
  - All query methods only need immutable guard (`guard_memvid!(self, py)`) since they're read-only
  - Separate write operations (memory.rs) from query operations (memory_query.rs) for clean module organization
---

## 2026-02-28 - US-013
- Wrapped memory card schema operations: register_schema, validate_card, set_schema_strict, infer_schemas
- Files changed:
  - Created bindings/python/src/schema.rs (PyPredicateSchema, PyValidationResult pyclasses, register_schema/validate_card/set_schema_strict/infer_schemas methods on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod schema;` and `schema::register(m)?;`)
  - Modified bindings/python/src/memory.rs (made `build_card` pub(crate) so schema module can reuse it)
- **Learnings for future iterations:**
  - `PredicateSchema::new(id, name)` creates a schema — set fields directly after creation (no builder chain needed for description, domain, etc.)
  - `ValueType` is a tagged enum: `String`, `Number`, `DateTime`, `Boolean`, `EntityRef { kind }`, `Enum { values }`, `Any` — decompose into separate Python kwargs: `range` (string), `range_entity_kind`, `range_enum_values`
  - `validate_card` on Memvid returns `Result<(), SchemaError>` — SchemaError implements `Display` so use `.to_string()` for the Python error message
  - `EntityKind` conversion: use `EntityKind::from_label()` or manual match — `from_label` accepts NER-style labels like "per", "org"; for Python API, prefer explicit full names
  - Reusing `build_card` from `memory.rs` requires making it `pub(crate)` — avoids duplicating card construction logic in the schema module
  - Dict helper functions (`dict_get_str`, etc.) are duplicated per module since they're not pub — could be moved to a shared utils module in future
---

## 2026-02-28 - US-014
- Wrapped logic mesh entity graph operations: add_mesh_node, add_mesh_nodes, add_mesh_edge, add_mesh_edges, follow, find_entity, frame_entities, entities_by_kind, logic_mesh_stats
- Files changed:
  - Created bindings/python/src/mesh.rs (PyMeshNode, PyFollowResult, PyLogicMeshStats pyclasses, 9 methods on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod mesh;` and `mesh::register(m)?;`)
- **Learnings for future iterations:**
  - `MeshNode::new` takes `(canonical_name, display_name, kind, confidence: f32, frame_id, byte_start, byte_len)` — for Python user-facing API, pass frame_id=0 and byte_start=0, byte_len=0 as placeholders
  - `MeshEdge::new` takes `(from_node: u64, to_node: u64, link: LinkType, confidence: f32, frame_id: FrameId)` — from_node/to_node are node IDs (u64), not names
  - `LinkType::from_str` is available and accepts flexible aliases (e.g., "manages" → Manager, "member_of" → Member) — use directly for Python string→LinkType conversion
  - `MeshNode.confidence` is stored as u8 (0-100) internally; use `confidence_f32()` for Python float representation
  - `LogicMeshStats.entity_kinds` and `link_types` are `HashMap<String, usize>` — use the `raw_*` + `#[getter]` pattern for HashMap fields in frozen pyclasses
  - `find_entity`, `frame_entities`, `entities_by_kind` return references (`&MeshNode` / `Vec<&MeshNode>`) — implement `From<&MeshNode>` for the PyMeshNode conversion
  - `follow` returns owned `Vec<FollowResult>` — implement `From<FollowResult>` (not reference)
---

## 2026-02-28 - US-015
- Wrapped ask/RAG operations: ask() method on PyMemvid with AskResponse, AskCitation, AskContextFragment, AskStats pyclasses
- Files changed:
  - Created bindings/python/src/ask.rs (PyAskStats, PyAskCitation, PyAskContextFragment, PyAskResponse pyclasses, ask() method on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod ask;` and `ask::register(m)?;`)
- **Learnings for future iterations:**
  - `ask()` method takes generic `Option<&E>` where `E: VecEmbedder` — create a dummy `NoEmbedder` struct and pass `None::<&NoEmbedder>` for basic usage without semantic embeddings
  - `AskResponse` contains a nested `SearchResponse` — reuse existing `PySearchResponse::from()` conversion via `clone()` since `SearchResponse` derives `Clone`
  - `AskCitation.chunk_range` is `Option<(usize, usize)>` in Rust — decompose to `chunk_range_start` and `chunk_range_end` as separate Python fields per PRD
  - `AskContextFragment.matches` maps to `match_count` in Python (same pattern as `SearchHit`)
  - `AskMode` is `#[serde(rename_all = "snake_case")]` so string values are "lex", "sem", "hybrid"
  - `AskRetriever` variants: Lex, Semantic, Hybrid, LexFallback, TimelineFallback — all mapped to snake_case strings
  - All `u128` timing fields (retrieval_ms, synthesis_ms, latency_ms) cast to `u64` for Python compatibility
  - `AskContextFragmentKind` has just two variants: Full and Summary — map to "full" and "summary" strings
---

## 2026-02-28 - US-016
- Wrapped sketch-based fast search: build_all_sketches, find_sketch_candidates, has_sketches, sketch_stats
- Files changed:
  - Created bindings/python/src/sketch.rs (PySketchTrackStats pyclass, 4 methods on PyMemvid, register function)
  - Modified bindings/python/src/lib.rs (added `pub mod sketch;` and `sketch::register(m)?;`)
- **Learnings for future iterations:**
  - `SketchVariant` is a simple discriminant enum (Small=0, Medium=1, Large=2) — manual match for string conversion, no serde needed
  - `find_sketch_candidates` returns `Vec<SketchCandidate>` with `frame_id`, `score`, etc. — PRD only wants `list[int]` (frame IDs), so extract `.frame_id` from each candidate
  - `SketchSearchOptions` is in `memvid_core::memvid::sketch` (not re-exported from types) — use full path for import
  - `build_all_sketches` needs `&mut self`; `has_sketches`, `sketch_stats`, `find_sketch_candidates` only need `&self`
  - Simple story — no complex type conversions, just direct wrapping of 4 Rust methods + 1 stats pyclass
---
